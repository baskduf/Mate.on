{
  "name": "@mateon/db",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "prisma format --schema prisma/schema.prisma",
    "generate": "prisma generate",
    "push": "prisma db push",
    "seed": "node prisma/seed.js",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.16.0"
  },
  "devDependencies": {
    "prisma": "6.16.0",
    "typescript": "5.9.3"
  }
}
